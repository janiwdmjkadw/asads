/**
 * Discover's UI is rendered by the persistent `PersistentDiscoverPane`
 * inside `TerminalShell` (it stays mounted across route changes so the
 * Trade→Discover return is a display flip instead of a full remount).
 * The route page exists only so /discover is a navigable URL.
 */
export default function DiscoverRoute() {
  return null;
}
