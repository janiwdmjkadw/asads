export type LineArtProps = {
  className?: string;
  /** Accessible name. Omit for decorative use — the SVG is then aria-hidden. */
  title?: string;
};

export type LineArtName = 'discover' | 'charts' | 'conditionals' | 'tracking';
