/*
 * The launchpad marks live in `source/` now — the real Discover column
 * draws them too, and two copies of fourteen traced logos is two copies
 * that drift.
 *
 * This file stays so the sheet's imports keep working.
 */
export { PAD_MARKS } from '@/components/discover/column/padMarks';
