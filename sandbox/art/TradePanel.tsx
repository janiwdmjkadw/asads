/*
 * The panel ships. It lives in `@/components/trade/TokenTradePanel` and
 * this sheet renders that same component, so `/whatever` and `/trade`
 * cannot show two different things.
 */
export { TokenTradePanel as TradePanel } from '@/components/trade/TokenTradePanel';
