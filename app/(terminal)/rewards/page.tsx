/*
 * Route shim. The page itself is the export's own file, re-exported from
 * `source/app/` so the tree under `source/` stays exactly as your friend
 * shipped it and remains droppable straight back into his repo.
 */
export { default } from '@/app/(terminal)/rewards/page';
